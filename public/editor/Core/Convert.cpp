#include "pch.h"

namespace RayCad
{
	/*
	void MarkLineseg(vector<int> *edges, shared_ptr<LineSeg> src, int index) {

		shared_ptr<LineSeg> cur = dynamic_pointer_cast<LineSeg>(src->Get(index));

		int num = src->GetNumberOfChildren();
		for (int i = 0; i < num; i++) {
			if (i == index)
				continue;

			shared_ptr<LineSeg> iline = dynamic_pointer_cast<LineSeg>(src->Get(i));

			iline->
		}
	}

	shared_ptr<PLine> Convert::FindEdges(vector<shared_ptr<LineSeg>> *list) {

		vector<int> edges;

		int num = src->GetNumberOfChildren();
		for (int i = 0; i < num; i++) {
			if (src->Get(i)->GetType() != (int)Geom::geom_type::G_LINE) {
				return NULL;
			}

			edges[i * 2] = 0;
			edges[i * 2+1] = 0;
		}



		return NULL;
	}
	*/
}